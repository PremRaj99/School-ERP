# Base URL of the SchoolERP API.
# The client appends /api/v1 automatically if it is not already present,
# so both of these are equivalent:
#   http://localhost:5000
#   http://localhost:5000/api/v1
VITE_API_URL=http://localhost:5000/api/v1
