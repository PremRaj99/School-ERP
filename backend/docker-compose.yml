services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '${PORT:-5000}:${PORT:-5000}'
    env_file:
      - .env
    # Optional: mount log directory if application writes logs to the disk
    volumes:
      - ./logs:/app/logs
